{% extends 'base02.html.twig' %}

{% block title %}Liste des termes publiables{% endblock %}

{% block body %}

<div class="accueil-sous-cont">

    <h1>Termes publiables</h1>
    <h3>Les termes publiables sont des textes juridiques qui informent des droits des utilisateurs</h3>

    <div class="sous-cont">

        <table class="documents-table">
            <thead>
                <tr>
                    <th>
                        Type
                    </th>
                    <th>
                        Date de mise à jour
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="clickable-row" data-href="{{ path('reglement_interieur_page') }}">
                        <a href="{{ path('reglement_interieur_page') }}">Règlement intérieur</a>
                    </td>
                    <td class="clickable-row" data-href="{{ path('reglement_interieur_page') }}">
                        <a href="{{ path('reglement_interieur_page') }}">12/02/2023</a>
                    </td>
                </tr>
                <tr>
                    <td class="clickable-row" data-href="{{ path('conditions_generales_utilisation_page') }}">
                        <a href="{{ path('conditions_generales_utilisation_page') }}">Conditions générales d'utilisation</a>
                    </td>
                    <td class="clickable-row" data-href="{{ path('conditions_generales_utilisation_page') }}">
                        <a href="{{ path('conditions_generales_utilisation_page') }}">18/06/2023</a>
                    </td>   
                </tr>
                <tr>
                    <td class="clickable-row" data-href="{{ path('conditions_generales_vente_page') }}">
                        <a href="{{ path('conditions_generales_vente_page') }}">Conditions générales de vente</a>
                    </td>
                    <td class="clickable-row" data-href="{{ path('conditions_generales_vente_page') }}">
                        <a href="{{ path('conditions_generales_vente_page') }}">15/02/2023</a>
                    </td>
                </tr>
            </tbody>
        </table>

    </div>  

</div>

{% endblock %}
